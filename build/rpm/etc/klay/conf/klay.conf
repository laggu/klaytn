# Configuration file for the klay service.

NETWORK_ID=8282

RPC_PORT=8765
WS_PORT=8767
PORT=22020

DATA_DIR=/var/klay/data
LOG_DIR=/var/log/klay
RUN_DIR=/var/run/klay

# NODE_TYPE [CN, PN, RN, GN]
NODE_TYPE=RN
